import ComponentsStock from '@/components/apps/stock/components-apps-contacts';
import { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'Propriedades',
};

const Contacts = () => {
    return <ComponentsStock />;
};

export default Contacts;
