import ComponentsProprieties from '@/components/apps/proprieties/components-apps-contacts';
import { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'Propriedades',
};

const Contacts = () => {
    return <ComponentsProprieties />;
};

export default Contacts;
